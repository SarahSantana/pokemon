import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  Text,
s} from 'react-native';

function App(): React.JSX.Element {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Hello World</Text>
      </ScrollView>
    </SafeAreaView>
  );
}

export default App;
